<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="product_quotation_tracking.list">
      <field name="name">product_quotation_tracking list</field>
      <field name="model">product_quotation_tracking.product_quotation_tracking</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="product_quotation_tracking.action_window">
      <field name="name">product_quotation_tracking window</field>
      <field name="res_model">product_quotation_tracking.product_quotation_tracking</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="product_quotation_tracking.action_server">
      <field name="name">product_quotation_tracking server</field>
      <field name="model_id" ref="model_product_quotation_tracking_product_quotation_tracking"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="product_quotation_tracking" id="product_quotation_tracking.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="product_quotation_tracking.menu_1" parent="product_quotation_tracking.menu_root"/>
    <menuitem name="Menu 2" id="product_quotation_tracking.menu_2" parent="product_quotation_tracking.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="product_quotation_tracking.menu_1_list" parent="product_quotation_tracking.menu_1"
              action="product_quotation_tracking.action_window"/>
    <menuitem name="Server to list" id="product_quotation_tracking" parent="product_quotation_tracking.menu_2"
              action="product_quotation_tracking.action_server"/>
    -->
  </data>
</odoo>